{
  "name": "@flyer-ui/commonality",
  "version": "0.0.2",
  "description": "项目开发中通用到的工具类",
  "main": "lib/index.cjs.js",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "test": "jest --no-cache",
    "build": "rollup -c",
    "clean": "rm -rf lib",
    "publish:patch": "npm run clean && npm run test && npm run build && npm version patch && git push --follow-tags && npm publish"
  },
  "keywords": [
    "commonality",
    "util"
  ],
  "author": "pfzhengd",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.3",
    "jest": "^26.1.0",
    "rollup": "^2.18.1",
    "rollup-plugin-typescript": "^1.0.1",
    "ts-jest": "^26.1.1",
    "tslib": "^2.0.0",
    "typescript": "^3.9.5"
  }
}
